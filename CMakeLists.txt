cmake_minimum_required(VERSION 3.1)
project(foobar CXX)
INCLUDE(CPack)
SET(MAJOR_VERSION 1)
SET(MINOR_VERSION 0)
SET(PATCH_VERSION 0)
 

#include(${CMAKE_CURRENT_SOURCE_DIR}/CPackConfig.cmake)
#include(CPack)
